```javascript
//// 효율성 탈락 코드

const getNum = (num) => {
    // const a = Math.min(10000000, Math.sqrt(num))
    let find = 1;
    for (let i = 2; i <= num; i++) {
        if(num / i <= 10000000 && num % i === 0) {
            return num / i ;
        }
    }
    return 1;
}

const solution = (begin, end) => {
    // 100, 120
    let answer = [];
    
    for (let i = begin; i <= end; i++) {
        const num = getNum(i)
        if(i === 1) {
            answer.push(0);
        } else {
            answer.push(num);
        }
    }
    
    return answer;
}

```


```javascript
// 통과




```
